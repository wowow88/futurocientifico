---
import assignaturas from '../data/assignaturas-2025-26.json'
---

<section class="mt-12 px-4 py-6 bg-gray-100 rounded-xl shadow-sm max-w-4xl mx-auto">
  <h2 class="text-2xl font-bold text-gray-800 mb-4">📚 Asignaturas 2025–26 y enlaces oficiales</h2>
  {assignaturas.map(curso => (
    <div class="mb-6">
      <h3 class="text-xl font-semibold text-gray-700 mb-2">{curso.curso}</h3>
      <ul class="list-disc list-inside text-gray-700">
        {curso.asignaturas.map(a => (
          <li>
            <strong>{a.nombre}</strong> – {a.tipo} – 
            <a href={a.enlace} target="_blank" rel="noopener" class="text-blue-600 underline">Ver currículo oficial</a>
          </li>
        ))}
      </ul>
    </div>
  ))}
</section>
