---
// Importa datos JSON
import videos from '../data/latest-videos.json'
---

<section class="mt-12 px-4 py-6 bg-gray-50 rounded-xl shadow-sm max-w-3xl mx-auto">
  <h2 class="text-2xl font-bold text-gray-800 mb-4">🎥 Video destacado de esta semana</h2>
  <ul class="list-disc list-inside text-blue-700">
    {videos.map(v => (
      <li>
        <a href={v.link} target="_blank" rel="noopener">
          [{v.channel}] {v.title}
        </a>
      </li>
    ))}
  </ul>
</section>
