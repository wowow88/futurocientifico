name: 📰 Daily Update

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  copy-articles:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repositorio destino (futurocientifico)
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Descargar archivo desde CurioScience
        run: |
          curl -o public/articles.json https://curioscience.vercel.app/articles.json

      - name: Commit y push de actualización
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add public/articles.json
          git commit -m "📰 Actualización diaria desde CurioScience [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
