name: 📰 Daily Update

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: 🔁 Actualización automática (simulada)
        run: echo "Ya no es necesario generar artículos. Se cargan desde CurioScience."

      - name: Fin
        run: echo "✅ Workflow completado."

