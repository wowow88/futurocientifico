name: 📚 Publicación automática diaria

on:
  schedule:
    - cron: '0 7 * * *'  # Todos los días a las 07:00 UTC
  workflow_dispatch:

jobs:
  update-content:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install feedparser beautifulsoup4

      - name: Ejecutar scripts de artículos científicos
        run: |
          python scripts/fetch_articles.py
          python scripts/merge_articles.py

      - name: Ejecutar EduFlash y actualizar eduflash.json
        run: |
          python scripts/eduflash_pipeline.py

      - name: Commit y push de todos los cambios
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add public/articles.json public/eduflash.json
          git commit -m "�
