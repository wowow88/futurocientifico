name: 📰 Copiar artículos desde CurioScience

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  copy-articles:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout FuturoCientífico
        uses: actions/checkout@v3

      - name: Descargar archivo articles.json desde CurioScience
        run: |
          curl -o public/articles.json https://raw.githubusercontent.com/wowow88/curioscience/workspace/astro/public/articles.json

      - name: Confirmar contenido del archivo
        run: cat public/articles.json

      - name: Commit and push
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add public/articles.json
          git commit -m "📰 Actualización diaria desde CurioScience [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main

