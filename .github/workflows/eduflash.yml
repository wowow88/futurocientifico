name: EduFlash - Actualización Semanal

on:
  schedule:
    - cron: "0 8 * * 5" # Ejecutar cada viernes a las 08:00 UTC
  workflow_dispatch:

jobs:
  update-eduflash:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Instalar dependencias Python
        run: |
          python -m pip install --upgrade pip
          pip install feedparser beautifulsoup4

      - name: Ejecutar script de EduFlash
        run: python scripts/eduflash_pipeline.py

      - name: Commit y push del archivo eduflash.json
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add public/eduflash.json
          git commit -m "🔄 Actualización semanal de EduFlash (viernes)" || echo "Sin cambios"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }} HEAD:main
